<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yanzu.module.member.dal.mysql.clearbill.ClearBillMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="sumMoneyByUserId" resultType="java.math.BigDecimal">
        select ifnull(sum(money), 0)
        from member_clear_bill
        where deleted = 0
          and user_id = #{userId}

    </select>
    <select id="getClearBillPage"
            resultType="com.yanzu.module.member.controller.app.clear.vo.AppClearBillRespVO">
        select id, user_id, money, order_num, order_ids, create_time
        from member_clear_bill
        where deleted = 0
          and user_id = #{reqVO.userId}
        order by create_time desc

    </select>
</mapper>
